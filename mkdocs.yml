site_name: MEBLOCK IOT | MicroPython API
site_description: Tài liệu thư viện thiết bị ESP32 MicroPython (MQTT, OLED, DHT)
site_url: https://<your-user>.github.io/<your-repo>
repo_url: https://github.com/<your-user>/<your-repo>
repo_name: <your-user>/<your-repo>

theme:
  name: material
  language: vi
  features:
    - navigation.tabs
    - navigation.sections
    - navigation.top
    - content.code.copy
    - search.suggest
    - search.highlight

plugins:
  - search

markdown_extensions:
  - admonition
  - attr_list
  - def_list
  - footnotes
  - pymdownx.details
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
  - pymdownx.tabbed:
      alternate_style: true
  - toc:
      permalink: true

nav:
  - 📚 Tổng quan: index.md
  - 1. MicroPython API:
      - oled_i2c — OLED I2C Module: api/oled_i2c.md
      - mqtt — MQTT Helper: api/mqtt.md
      - aiot_dht — DHT Sensors: api/aiot_dht.md
  - Hướng dẫn:
      - Xử lý lỗi: guides/troubleshooting.md
